# HAKO 아트 · 사운드 가이드

> 한 줄 방향: **따뜻한 테라리움 속 작은 친구.**
> 고급스러움 = 절제(색 수·장식 최소, 여백) · 귀여움 = 비율과 표정 · 재미 = 누를 때마다 오는 반응.

이 문서는 최종 아트·사운드를 외주하거나 직접 그릴 때의 기준이다. 코드에 들어간 수치(프록시 그림, 연출 색)도 이 표를 따른다.
> **게코·장식 그림을 새로 만들 때는 `ART_ORDER_GECKO.md`(AI 그림 생성 주문서 — 캔버스 규격·프롬프트·자르기·넣는 법)를 함께 본다.**

---

## 1. 팔레트

한 화면의 주요 색은 **5~6개**로 제한한다. 순수 검정(#000)과 순수 원색은 쓰지 않는다.

### 게코 (크레스티드, 살구색 모프 — 프록시 기준)

| 용도 | HEX | 비고 |
|------|-----|------|
| 몸통 | `#E3A86A` | 살구 |
| 머리 | `#EAB476` | 몸통보다 한 톤 밝게 |
| 다리 · 꼬리 | `#DDA065` · `#DB9E63` | 먼 쪽 다리는 84% 밝기 |
| 배 · 주둥이 | `#F7E3C2` · `#F2CD99` | 크림 |
| 점무늬 | `#C98348` | 캐러멜 |
| 외곽선 | `#6B4330` | **검정 대신 짙은 갈색** — 부드러운 인상 |
| 눈 | 홍채 `#F3DA9A` / 테 `#D6A95E` / 동공 `#2B1D14` | 흰 하이라이트 2개 |
| 볼터치 | `#FF8C8C` 38% | 눈 아래 앞쪽 뺨 |
| 혀 | `#F29AA8` / 선 `#B8606F` | |

### UI · 연출

| 용도 | HEX | 쓰는 곳 |
|------|-----|---------|
| 바탕 크림 | `#FFF8EE` | 씬 전환 페이드, 밝은 패널 |
| 글자 갈색 | `#5B3A29` | 말풍선 글자, 본문 |
| 하트 핑크 | `#FF8FA3` → `#FFB8C4` | 쓰다듬기 |
| 반짝 금색 | `#FFE89E` | 청소·성장·허물 |
| 물방울 | `#8FD4FF` → `#CCEEFF` | 분무 |
| 경고 | `#FF4545` ↔ `#FF8C8C` (천천히 깜빡임) | 위험 게이지 |

---

## 2. 선 · 명암

- 외곽선: 스킨 픽셀 기준 **3~4px**, 색은 `#6B4330`. 안쪽 디테일 선은 `#9A6B4E`로 한 단계 연하게.
- 명암: **2단계 + 부드러운 그라데이션** (2026-09-18 수정 — 예전 "1단계만"은 그려진 배경과 어울리지 않았다). 위를 밝게, 아래를 어둡게 하고 경계를 살짝 풀어 준다. 강한 광택은 쓰지 않는다.
- 하이라이트: 눈·물방울·하트에 작은 흰 점 1~2개. 몸에는 넣지 않는다.
- 그림자: 발밑 타원 한 개(검정 33%, 가장자리 부드럽게). 게코를 덮지 않도록 맨 뒤 레이어.

---

## 3. 귀여움 공식 — 성장 단계별 비율

어릴수록 **머리·눈이 크고 꼬리가 짧다.** 같은 그림을 코드로 변형해 만든다(`GeckoProxyArt.GenerateStageSkins`). 최종 아트도 이 비율을 맞추거나, 단계별 그림을 따로 그려 `GeckoRig > Stage Skins`에 넣는다.

| 단계 | 화면 크기 (`_stageScales`) | 머리 | 눈 (머리 대비 추가) | 꼬리 길이 |
|------|:---:|:---:|:---:|:---:|
| 0 Hatchling | 0.55 | ×1.22 | ×1.12 | ×0.88 |
| 1 Baby | 0.68 | ×1.22 | ×1.12 | ×0.88 |
| 2 Juvenile | 0.80 | ×1.10 | ×1.05 | ×0.95 |
| 3 Sub-Adult | 0.90 | 기본 | 기본 | 기본 |
| 4 Adult | 1.00 | 기본 | 기본 | 기본 |

---

## 4. 표정

눈 9종 · 입 8종. 이름은 코드와 **글자 단위로 일치**해야 한다 (`GeckoParts.cs`).

| 눈 | 쓰는 때 | 입 | 쓰는 때 |
|----|---------|----|---------|
| `eye_open` | 기본 | `mouth_closed` | 기본 |
| `eye_look_left` / `eye_look_right` / `eye_look_up` | 둘러보기, 걷기 | `mouth_smile` | 기쁨 |
| `eye_closed` | 거절("흥"), 졸기 | `mouth_open_small` | 혀 내밀기 |
| `eye_sleepy` | 졸림 기분 | `mouth_open_wide` | 먹이 받기, 성장 |
| `eye_happy` | 쓰다듬기, 먹은 뒤 | `mouth_chew` | 오물오물 |
| `eye_surprised` | 놀람, 허물 시작 | `mouth_drink` | 물 마시기 |
| `eye_sparkle` | 기쁨, 성장, 눈 닦은 뒤 | `mouth_surprised` / `mouth_frown` | 놀람 / 화남·거절 |

> 크레스티드 게코는 눈꺼풀이 없다(혀로 눈을 닦는 게 시그니처). `eye_closed`·`eye_sleepy`의 눈꺼풀은 **게임적 허용**. 레오파드처럼 눈꺼풀이 있는 종은 `GeckoSpeciesSO.canBlink`를 켠다.

---

## 5. 파츠 규격 (최종 아트 발주용)

- **오른쪽을 보는 그림**, 좌표 원점 = **발밑 중앙**. 좌우 반전은 코드가 한다.
- 레이어 14개 (그리는 순서, 뒤 → 앞):
  `shadow_contact` · `tail` · `leg_back_far` · `leg_front_far` · `body` · `shed_patch` · `leg_back_near` · `leg_front_near` · `head` · `eye_l` · `eye_r` · `mouth` · `tongue_01` · `tongue_02`
- 표정 레이어: 위 4장 표의 이름 그대로 (`eye_r_…`로 오른쪽 눈만 따로 줄 수 있다).
- 혀는 두 마디. 입 안에 접힌 채(오른쪽으로 뻗는 방향) 그린다.
- 꼬리는 **곧게 펴서** 그린다 — 휘는 건 코드(`GeckoBendGraphic`)가 한다.
- 내보내기 우선순위:
  1. **PSD + Unity 임포트 설정 "Character Rig"** — 레이어 위치를 그대로 읽는다 (권장)
  2. 파츠별로 잘라 낸 PNG + 관절 위치는 Scene 뷰에서 조정
  3. 캔버스 통째로 내보낸 PNG — 동작은 하지만 투명 여백까지 그려서 모바일 부담이 크다 (비권장)
- 크기: 어른 게코 전체 폭(꼬리 끝~주둥이) **1,000~1,500px**. 화면에서는 약 640 UI 폭으로 줄어든다.
  2배 넘게 줄어드는 그림은 텍스처 임포트에서 **Generate Mip Maps**를 켜야 가장자리가 반짝거리지 않는다.
- 적용: Project 창에서 PSD/폴더 선택 → `Hako > Gecko > ② 스킨 만들기` → 만든 스킨 선택 → `③ 씬 게코에 적용`.

---

## 6. UI 규칙

- 모서리: 둥글게 (반지름 24~40 UI). 딱딱한 직각 패널은 쓰지 않는다.
- 그림자 대신 **부드러운 테두리**(따뜻한 회색 `#E6D9CC`, 2~3px).
- 강조색은 한 화면에 **하나**. 나머지는 크림·갈색 계열.
- 여백: 화면 가장자리 48 UI 이상. 버튼끼리 24 UI 이상.
- 버튼 반응은 코드가 준다 — 눌림 0.92배 + 튕김 + '톡' 소리 (`UIPressScale`, 모든 Button에 자동).
- 게이지는 부드럽게 차오르고, 오를 때 살짝 부푼다. 위험 구간은 천천히 깜빡인다 (`HomeUIController.GaugeView`).

---

## 7. 글꼴

현재 `NanumGothic-Regular SDF`는 **정적 아틀라스**라 한글 11,172자 + 영문·숫자·기본 기호만 들어 있다.
`★ ♥ → … ✦` 같은 특수문자는 □로 나온다 → **UI 문구에 특수문자를 쓰지 않는다.**

고급 + 귀여움에는 **둥근 계열 글꼴**을 권장한다.
- 예: 나눔스퀘어라운드 (네이버 나눔 글꼴, SIL OFL 계열 — 상업 이용 전 라이선스 원문 확인)
- 적용: Window → TextMeshPro → Font Asset Creator → Character Set "Characters from File"(자주 쓰는 한글 2,350자 + 필요한 기호) 또는 Dynamic 아틀라스 → `Assets/Editor/FontApplier.cs`로 일괄 교체.
- 특수문자를 쓰고 싶으면 새 글꼴 에셋에 해당 글자를 포함시킨다.

---

## 8. 사운드

방향: **맑은 종소리 · 부드러운 미끄럼음 · 걸러낸 잡음.** 날카로운 전자음, 큰 타격음은 쓰지 않는다.
지금은 코드로 합성한 임시 소리(`SfxSynth`)가 나온다. 파일을 넣으면 그 파일이 우선이다.

| 파일 이름 (`Resources/Audio/Sfx/`) | 쓰는 곳 |
|------|---------|
| `tap` | 모든 버튼 |
| `pop` | 먹이 등장, 게코 선택 |
| `lick` / `crunch` | 혀 '쪽' / 오물오물 |
| `spray` / `drip` | 분무 / 물방울 |
| `heart` | 쓰다듬기 |
| `sparkle` | 청소, 꾸미기 적용, 보상 |
| `chime` | 성장, 분양 |
| `moltsuccess` / `moltfail` | 허물 |
| `coin` | 재화 증가, 구매 |
| `refuse` / `annoyed` | 거절 / 삐짐 |
| `boing` | 점프 |
| `error` | 구매 실패 |

- 배경음: `Resources/Audio/Bgm/home` (없으면 테라리움 환경음 — 잎사귀 바람·물방울·먼 풀벌레 — 을 합성)
- 형식: 효과음 `.wav`(짧음), 배경음 `.ogg`(Load Type: Streaming). 효과음 최대 음량은 -6 dBFS 정도로 여유 있게.

---

## 9. 연출 그림 교체

`Resources/Fx/`에 같은 이름의 스프라이트를 넣으면 코드로 그린 그림 대신 쓴다.
`heart` · `drop` · `sparkle` · `dot` · `flake` · `puff` · `ring` · `bug` · `bubble`(9-slice, 테두리 44px) · `bubble_tail`
흰색 바탕에 명암만 넣어 두면 색은 코드가 입힌다 (`bug`·`bubble` 제외).

---

## 10. 앱 아이콘

현재 아이콘은 임시로 코드로 그린 것이다 — 초록 배경 + 크림색 원 + 게코 얼굴(반짝이는 눈 · 미소 · 볼터치).

- 파일: `Assets/_Game/Textures/Icons/app_icon.png` (1024×1024, 불투명)
- 연결: Player Settings → Android → Icon의 **Legacy · Round** 슬롯 (적응형은 비워 두어 Unity가 자동 생성)
- 최종 아이콘 규칙: 얼굴이 화면의 **75% 안쪽**에 들어와야 런처가 모서리를 깎아도 잘리지 않는다. 글자를 넣지 않는다.
- 스토어용으로 512×512(구글플레이 등록용)도 같은 그림으로 준비한다.

## 11. 외주 체크리스트

- [ ] 오른쪽을 보는 그림, 발밑 중앙 원점
- [ ] 레이어 이름 14개 + 표정 17개 정확히
- [ ] 외곽선 짙은 갈색, 명암 1단계
- [ ] 단계별 비율(3장) 반영 또는 단계별 그림 별도 제공
- [ ] PSD(Character Rig) 또는 파츠별 PNG
- [ ] 효과음 16종 이름 일치, 배경음 1곡(반복 이음매 없게)
